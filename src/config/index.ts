export const {
  PORT,
  DATABASE_URL,
  GITHUB_CLIENT_ID,
  GITHUB_CLIENT_SECRET,
  JWT_SECRET,
  GOOGLE_CLIENT_ID,
  GOOGLE_CLIENT_SECRET,
  GMAIL_USER,
  GMAIL_PASSWORD,
  MAILER_HOST,
  MAILER_SERVICE,
  MAILER_PORT,
  MAILER_SECURE,
  AWS_ACCESS_KEY,
  AWS_SECRET_ACCESS_KEY,
  AWS_BUCKET,
  AWS_REGION,
  REDIS_HOST,
  REDIS_PORT,
  REDIS_USERNAME,
  REDIS_PASSWORD,
  KAFKA_HOST,
  KAFKA_PORT,
  KAFKA_USERNAME,
  KAFKA_PASSWORD,
  KAFKA_CA_PATH,
} = process.env;
